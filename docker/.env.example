# 复制为 docker/.env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）
# docker compose 会读取本文件进行变量注入
MYSQL_ROOT_PASSWORD=your-mysql-root-password
DB_USERNAME=root
DB_PASSWORD=your-db-password
REDIS_PASSWORD=your-redis-password
