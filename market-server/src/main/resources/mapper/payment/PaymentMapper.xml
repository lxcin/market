<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.market.module.payment.mapper.PaymentMapper">

    <update id="markPaid">
        UPDATE t_payment
        SET status = 1, trade_no = #{tradeNo}, notify_time = NOW()
        WHERE payment_no = #{paymentNo} AND status = 0
    </update>

    <update id="markFailed">
        UPDATE t_payment
        SET status = 2, notify_time = NOW()
        WHERE payment_no = #{paymentNo} AND status = 0
    </update>

    <update id="markRefunded">
        UPDATE t_payment
        SET status = 4
        WHERE payment_no = #{paymentNo} AND status = 1
    </update>

</mapper>
